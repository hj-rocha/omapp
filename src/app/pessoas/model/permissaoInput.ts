export class PermissaoInput{

/**  constructor(
    id: number,
    nome: string,
    descricao: string,
    pertence: boolean

  ){
    this.id = id;
    this.nome=nome;
    this.descricao = descricao;
    this.pertence = pertence;
  };
*/
  id: number;
  grupoNome: string;
  nome: string;
  descricao: string;
  pertence:boolean;
}
